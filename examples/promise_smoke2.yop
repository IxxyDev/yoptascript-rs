гыы p = СловоПацана.решить(10);
p.потом((v) => { сказать("первый:", v); });
сказать("done");

гыы all = СловоПацана.всех([
    СловоПацана.решить(1),
    СловоПацана.решить(2),
    СловоПацана.решить(3)
]);
all.потом((v) => { сказать("всех:", v); });

гыы x = 99;
гыы y = 99;
сказать(x + y);
