гыы массив = [];

го (гыы и = 0; и < 500; и++) {
    массив.push(и);
}

гыы сумма = 0;

го (гыы к = 0; к < длина(массив); к++) {
    вилкойвглаз (массив[к] % 3 === 0) {
        сумма += массив[к] * 2;
    }
}
