гыы лог = "";

{
    юзай сидетьНахуй р = { асинхРасход: () => { лог = лог + "р;"; } };
}
сказать(лог);

лог = "";
{
    юзай а = { расход: () => { лог = лог + "а"; } };
    юзай сидетьНахуй б = { асинхРасход: () => { лог = лог + "б"; } };
    юзай в = { расход: () => { лог = лог + "в"; } };
}
сказать(лог);

лог = "";
{
    юзай сидетьНахуй р = { расход: () => { лог = лог + "sync-fallback;"; } };
}
сказать(лог);

лог = "";
{
    юзай сидетьНахуй р = {
        асинхРасход: ассо йопта() {
            лог = лог + "начало;";
            сидетьНахуй СловоПацана.решить(0);
            лог = лог + "конец;";
        }
    };
    лог = лог + "тело;";
}
лог = лог + "после;";
сказать(лог);

лог = "";
хапнуть {
    юзай сидетьНахуй р = {
        асинхРасход: ассо йопта() { кидай "бабах"; }
    };
} гоп (е) {
    лог = лог + "пойман:" + е + ";";
}
сказать(лог);

лог = "";
{
    гыы р = {};
    р[Симбол.асинхРасход] = () => { лог = лог + "symbol-async;"; };
    юзай сидетьНахуй рес = р;
}
сказать(лог);

лог = "";
{
    юзай сидетьНахуй п = ноль;
    лог = лог + "нолл-пропущен;";
}
сказать(лог);
