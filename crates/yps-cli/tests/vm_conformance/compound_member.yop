гыы о = { счёт: 10 };
о.счёт += 5;
сказать(о.счёт);
о.счёт *= 2;
сказать(о.счёт);
о.счёт -= 3;
сказать(о.счёт);
о.счёт /= 3;
сказать(о.счёт);

гыы а = [1, 2, 3];
а[0] += 100;
а[2] *= 10;
сказать(а);
гыы и = 1;
а[и] -= 1;
сказать(а);
