гыы лог = "";

{
    юзай а = { расход: () => { лог = лог + "а"; } };
    юзай б = { расход: () => { лог = лог + "б"; } };
    юзай в = { расход: () => { лог = лог + "в"; } };
    лог = лог + "тело;";
}
сказать(лог);

{
    юзай п = ноль;
    сказать("нолл пропущен");
}

клёво Файл {
    Файл(имя) {
        тырыпыры.имя = имя;
    }
    расход() {
        сказать("закрыт " + тырыпыры.имя);
    }
}

{
    юзай ф = захуярить Файл("данные");
    сказать("работаю");
}
сказать("готово");
