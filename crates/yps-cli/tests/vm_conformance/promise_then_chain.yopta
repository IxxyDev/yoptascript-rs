СловоПацана.решить(1)
    .потом((v) => v + 1)
    .потом((v) => v * 10)
    .потом((v) => {
        сказать("цепочка:", v);
        отвечаю v;
    })
    .наконец(() => сказать("наконец"))
    .потом((v) => сказать("после наконец:", v));

сказать("старт");
