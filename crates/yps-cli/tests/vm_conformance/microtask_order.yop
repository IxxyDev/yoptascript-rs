сказать("1 синхронно");

СловоПацана.решить("A").потом((v) => {
    сказать("3 микро", v);
    СловоПацана.решить("B").потом((w) => сказать("5 микро", w));
});

сразу(() => сказать("4 сразу"));

сказать("2 синхронно");
