сказать("aaa".заменить(/a*/g, "-"));
сказать("abc".заменить(/x*/g, "-"));
сказать("aaa".заменить(/a*/, "-"));
сказать("hello".заменитьВсе(/(?:)/g, "."));
гыы cnt = 0;
го (м сашаГрей "abc".найтиВсе(/x*/g)) {
    cnt = cnt + 1;
}
сказать(cnt);
гыы parts = "abc".split(/(?:)/);
сказать(parts.длина + " :: " + parts.join("|"));
сказать("a1b".заменить(/\d*/g, "#"));
