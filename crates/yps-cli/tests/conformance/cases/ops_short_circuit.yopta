гыы лог = [];
йопта поб(м, в) { лог.втолкнуть(м); отвечаю в; }
гыы р1 = поб("a", лож) && поб("b", правда);
гыы р2 = поб("c", правда) || поб("d", правда);
сказать(лог.join(","));
сказать(р1 + " " + р2);
