клёво Пёс {
  конструктор(имя) {
    тырыпыры.имя = имя;
  }
  голос() {
    сказать("Гав");
  }
}

гыы obj = Кент.создать(Пёс.prototype);

сказать(obj шкура Пёс);
сказать(Кент.прототип(obj) === Пёс.prototype);
сказать(obj.конструктор === Пёс);

obj.голос();
