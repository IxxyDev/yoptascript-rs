гыы лог = [];
йопта ф(бросать) {
  хапнуть {
    лог.втолкнуть("try");
    вилкойвглаз (бросать) { кидай "бум"; }
  } гоп(е) {
    лог.втолкнуть("catch:" + е);
  } тюряжка {
    лог.втолкнуть("finally");
  }
}
ф(лож);
ф(правда);
сказать(лог.join(","));
