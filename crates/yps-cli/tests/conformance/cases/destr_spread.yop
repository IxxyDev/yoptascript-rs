гыы а = [1, 2];
гыы б = [0, ...а, 3];
сказать(б.join(","));
йопта сумма(...числа) {
  гыы с = 0;
  го (гыы н сашаГрей числа) { с += н; }
  отвечаю с;
}
сказать(сумма(...б));
сказать(сумма(1, 2, 3));
гыы вместе = [...а, ...б];
сказать(вместе.join(","));
