гыы sym = Симбол("сек");
гыы src = {};
src[sym] = 99;

гыы via_spread = { ...src };
сказать(Кент.ключи(via_spread).длина);

гыы target = {};
Кент.назначить(target, src);
сказать(Кент.ключи(target).длина);

сказать(via_spread[sym]);
сказать(target[sym]);
