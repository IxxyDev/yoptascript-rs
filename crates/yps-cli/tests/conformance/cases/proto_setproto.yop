гыы parent = { x: 42 };
гыы child = Кент.создать(parent);

сказать(child.x);
сказать(Кент.прототип(child) === parent);

гыы newParent = { x: 99 };
Кент.назначитьПрототип(child, newParent);

сказать(child.x);
сказать(Кент.прототип(child) === newParent);

Кент.назначитьПрототип(child, ноль);
сказать(Кент.прототип(child));
