клёво База {
  приветствие() {
    сказать("Привет от Базы");
  }
  тип() {
    сказать("База");
  }
}

гыы obj = Кент.создать(База.prototype);
obj.приветствие();
obj.тип();

сказать(obj шкура База);
сказать(Кент.прототип(obj) === База.prototype);
