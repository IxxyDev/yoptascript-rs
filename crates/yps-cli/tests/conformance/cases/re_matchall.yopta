го (м сашаГрей "a1 b2 c3".найтиВсе(/([a-z])(\d)/g)) {
    сказать(м[0] + " | " + м[1] + " | " + м[2] + " | " + м.index);
}
сказать("---match no g---");
гыы m = "abc 123".совпадает(/(\d+)/);
сказать(m[0]);
сказать(m[1]);
сказать(m.index);
сказать("---match with g---");
гыы all = "a1 b2 c3".совпадает(/\d/g);
сказать(all.длина);
сказать(all.join(","));
сказать("---match no result---");
сказать("abc".совпадает(/\d/));
сказать("---matchAll empty---");
гыы cnt = 0;
го (м2 сашаГрей "xyz".найтиВсе(/\d/g)) {
    cnt = cnt + 1;
}
сказать(cnt);
