пиздюли счёт() {
    хапнуть {
        гыы и = 0;
        потрещим (правда) {
            поебалу и;
            и = и + 1;
        }
    } тюряжка {
        сказать("генератор закрыт");
    }
}
гыы сумма = 0;
го (гыы х сашаГрей счёт()) {
    вилкойвглаз (х >= 3) {
        харэ;
    }
    сумма = сумма + х;
}
сказать("сумма:", сумма);

гыы собрано = [];
го (гыы х сашаГрей счёт()) {
    собрано.втолкнуть(х);
    вилкойвглаз (собрано.длина >= 2) {
        харэ;
    }
}
сказать("собрано:", собрано[0], собрано[1]);
