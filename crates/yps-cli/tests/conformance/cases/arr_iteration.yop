гыы счёт = 0;
[10, 20, 30].some((эл, и, мас) => {
    счёт = счёт + 1;
    сказать(эл + ":" + и + ":" + мас.length);
    отвечаю эл > 15;
});
сказать(счёт);

счёт = 0;
[10, 20, 30].every((эл, и, мас) => {
    счёт = счёт + 1;
    сказать(эл + ":" + и + ":" + мас.length);
    отвечаю эл < 25;
});
сказать(счёт);

счёт = 0;
[10, 20, 30].find((эл, и, мас) => {
    счёт = счёт + 1;
    сказать(эл + ":" + и + ":" + мас.length);
    отвечаю эл > 15;
});
сказать(счёт);

счёт = 0;
[10, 20, 30].findIndex((эл, и, мас) => {
    счёт = счёт + 1;
    сказать(эл + ":" + и + ":" + мас.length);
    отвечаю эл > 15;
});
сказать(счёт);

счёт = 0;
[10, 20, 30].findLast((эл, и, мас) => {
    счёт = счёт + 1;
    сказать(эл + ":" + и + ":" + мас.length);
    отвечаю эл < 25;
});
сказать(счёт);

счёт = 0;
[10, 20, 30].findLastIndex((эл, и, мас) => {
    счёт = счёт + 1;
    сказать(эл + ":" + и + ":" + мас.length);
    отвечаю эл < 25;
});
сказать(счёт);
