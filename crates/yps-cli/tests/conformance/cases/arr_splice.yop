гыы а = [1, 2, 3, 4, 5];
гыы вырезано = а.splice(1, 2);
сказать(вырезано.join(","));
сказать(а.join(","));

гыы б = [1, 2, 3, 4, 5];
гыы вст = б.splice(2, 0, 10, 11);
сказать(вст.join(","));
сказать(б.join(","));

гыы в = [1, 2, 3, 4, 5];
гыы зам = в.splice(1, 2, 99);
сказать(зам.join(","));
сказать(в.join(","));

гыы г = [1, 2, 3, 4, 5];
гыы хвост = г.splice(-2);
сказать(хвост.join(","));
сказать(г.join(","));

гыы д = [1, 2, 3, 4, 5];
гыы пусто = д.splice(2);
сказать(пусто.join(","));
сказать(д.join(","));

гыы е = [1, 2, 3];
гыы много = е.splice(1, 100);
сказать(много.join(","));
сказать(е.join(","));

гыы ж = [1, 2, 3];
гыы пустой = ж.splice(1, 0);
сказать(пустой.length);
сказать(ж.join(","));
